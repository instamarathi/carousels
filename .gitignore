node_modules
dist
.DS_Store
*.log
.claude
tsconfig.tsbuildinfo

# Local build pipeline (paths to private source repo, free-series list).
# Data is committed pre-built; the workflow only runs `npm run build`.
build.py
*.py
__pycache__/
